<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.thesis</groupId>
        <artifactId>thesis-sentiment-shop</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>web</artifactId>
    <name>web</name>
    <description>
        REST controllers, security configuration, Spring Boot main.
        This is the only module that knows about every other module and
        also the only module that depends on a concrete classifier
        implementation, which is selected on build time via Maven profiles.
    </description>

    <dependencies>
        <dependency>
            <groupId>com.thesis</groupId>
            <artifactId>catalog</artifactId>
        </dependency>
        <dependency>
            <groupId>com.thesis</groupId>
            <artifactId>reviews</artifactId>
        </dependency>
        <dependency>
            <groupId>com.thesis</groupId>
            <artifactId>sample-data</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!--
            stub — default profile. Wires the no-op classifier so the application boots without any
            model artefact present. Activated automatically unless another profile is selected.
        -->
        <profile>
            <id>stub</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>com.thesis</groupId>
                    <artifactId>inference-stub</artifactId>
                </dependency>
            </dependencies>
        </profile>

        <!--
            e-sync — embedded synchronous variant.
            Requires the ONNX artefact to be available at the path referenced by SENTIMENT_MODEL_PATH.
        -->
        <profile>
            <id>e-sync</id>
            <dependencies>
                <dependency>
                    <groupId>com.thesis</groupId>
                    <artifactId>inference-embedded-sync</artifactId>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
